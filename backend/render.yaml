# Render.com Build Configuration
# This file tells Render how to build and run the backend

# Build command
build:
  - pip install -r requirements.txt

# Start command
start: gunicorn app:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
